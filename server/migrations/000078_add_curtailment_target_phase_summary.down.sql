ALTER TABLE curtailment_target
    DROP CONSTRAINT IF EXISTS ck_curtailment_target_phase_counts,
    DROP CONSTRAINT IF EXISTS ck_curtailment_target_restore_state,
    DROP CONSTRAINT IF EXISTS ck_curtailment_target_curtail_state,
    DROP COLUMN IF EXISTS restore_last_error,
    DROP COLUMN IF EXISTS restore_failure_count,
    DROP COLUMN IF EXISTS restore_retry_count,
    DROP COLUMN IF EXISTS restore_completed_at,
    DROP COLUMN IF EXISTS restore_batch_uuid,
    DROP COLUMN IF EXISTS restore_dispatched_at,
    DROP COLUMN IF EXISTS restore_started_at,
    DROP COLUMN IF EXISTS restore_state,
    DROP COLUMN IF EXISTS curtail_last_error,
    DROP COLUMN IF EXISTS curtail_failure_count,
    DROP COLUMN IF EXISTS curtail_retry_count,
    DROP COLUMN IF EXISTS curtail_completed_at,
    DROP COLUMN IF EXISTS curtail_batch_uuid,
    DROP COLUMN IF EXISTS curtail_dispatched_at,
    DROP COLUMN IF EXISTS curtail_state;
